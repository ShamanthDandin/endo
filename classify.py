import random

def classify_image(file_path):
    # use the file_path and send it to the model and have it return back the ans
    # return a simulated result
    result = {
        "message" : "Everything done"
    }
    
    return result
